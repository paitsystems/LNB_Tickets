<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/logout"
        android:icon="@drawable/logout_"
        android:title="@string/logout"
        app:showAsAction="never"/>

    <item android:id="@+id/refresh"
        android:title="@string/refresh"
        android:icon="@drawable/refresh"
        app:showAsAction="never"/>

    <item android:id="@+id/clear_data"
        android:title="@string/clear_data"
        app:showAsAction="never"/>

    <item android:id="@+id/changePassword"
        android:icon="@drawable/logout_"
        android:title="@string/change_password"
        app:showAsAction="never"/>

    <item android:id="@+id/report_error"
        android:title="@string/report_error"
        app:showAsAction="never"/>

    <item android:id="@+id/reach_to_md"
          android:title="@string/reach_to_md"
          app:showAsAction="never"/>
</menu>